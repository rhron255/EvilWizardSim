# Sprint notes — `claude/major-feature-sprint-42l55c`

Working notes for the 24h sprint. Updated at every checkpoint so a 6-hour
check-in can recover state without re-deriving it.

## The work

Flagship: **issue #6** — all 32 artifacts are `Defense +N`, so the 32-slot
collection grid is one stat in thirty-two costumes. Give relics a real second
axis. Issue #42 names #6 as one of the two plausible levers for its failing
balance gates, so the two land together.

Bundled, because they share a surface and a measurement:

- **#41 [Critical]** — clamped-to-zero costs hand broke players free
  legendaries (failure mode 14). Same relic economy; fixing it separately
  would force two balance re-measurements.
- **#42 [High]** — `slain_by_chosen_one` over its 45% ceiling, lichdom-seeker
  reachability under its 2% floor.

## Baseline (main @ 816902a, measured, not assumed)

Four gates green. `npm run sim`, 2000 runs, base seed 1 — three FAILs:

```
[FAIL] No single ending above 45%          slain_by_chosen_one 50.90%
[FAIL] Lichdom reachable by a lich-seeker (2-15% of cohort)  1.54%
[FAIL] Rarity ordering                     eternally_repurposed 0.50% < good_wizard 1.40%
```

Ending mix: slain 50.90 · sealed_in_gem 18.35 · exiled 9.90 · swamp 8.85 ·
fertilizer 5.75. The top two are both "somebody kills you" and are 69% between
them. Full baseline: `scratchpad/baseline-sim.txt`.

## The design

`Artifact.defense: number` and `Artifact.effect: string` both go. One field
replaces them:

```ts
Artifact.power: ArtifactPower   // required, exactly one
```

`effect` is DERIVED from `power` by one renderer, so the prose can never
disagree with the number (failure mode 4 is a hand-written mirror of exactly
this shape). Six power kinds, each a modifier of a system that already exists
— no new era-end tick, so no new disclosure channel is invented:

| power | what it does | lever | disclosed by |
|---|---|---|---|
| `wards` | Defense +v | `defenseOf` | the wards readout's Relics term |
| `vigil` | hero threat rises v less each decline era | `threatGainFor` | `his threat +N an era` |
| `undimmed` | notoriety decays v less each decline era | `decayFor` | the relic card only — the erosion is deliberately quiet (rule 5) |
| `discipline` | loyalty drifts v less each decline era | drift in `run.ts` | the `loyaltyDrift` systemic row |
| `haggle` | every follower cost is v smaller | `applyEffects` | `projectEffects` prints the reduced cost |
| `grace` | every standing LOSS is v smaller | `applyStanding` | `projectEffects` prints the softened delta |

`haggle`/`grace` need no new disclosure at all: they change what `applyEffects`
does, and `projectEffects` IS `applyEffects` against a draft, so the offer card
prints the real number for free.

## Acceptance bar

All 17 sim targets PASS (baseline has 3 FAILs), four gates green, mobile QA at
393x852 read back.

## Progress

- [x] Baseline measured, deps installed
- [x] Phase A — #41 gates + widened validator (commit 3658efc)
- [x] Phase B — #6 artifact powers, built and gated green
- [ ] Phase C — #42 tune to green  ← HERE
- [ ] Phase D — review + simplifications

### After Phase A (measured)

Gating closed routes, as failure mode 14 warns: Ascension 1.15% -> 0.65%
(legendary-holding 7.45% -> 5.00%), full completion 846.5 -> 1021, lichdom
seeker 1.54% -> 0.00%, slain 50.90% -> 47.80%. Five FAILs. Deliberately left
for the single tuning pass in Phase C — the prices on the newly-gated cards
were authored when roughly half of them charged nothing, so their real cost is
now well above what was tuned.

### Phase B notes

- `Artifact.defense` and `Artifact.effect` are both GONE, replaced by one
  `power: ArtifactPower`. The card line is derived (`artifactPowerText`).
- Disclosure landed in three places: the relic card (collection), the
  acquisition row on the resolution overlay (new — a relic used to be handed
  over with a name and a joke and nothing about what it does), and the header's
  Relics caption, which now prints the SUMMED powers instead of claiming every
  relic is defence.
- `siegeFor`/`stakesFor` now need content-derived data, so `App` derives
  `siege` and `relics` and `DecisionPanel` went fully presentational.
- Two floors added with provenance: `HERO_THREAT_MIN`, `LOYALTY_DRIFT_MIN`.
  Both are rule 6 (an ending must stay reachable), not balance targets.
- 27 new tests. All five engine behaviours mutation-tested and caught.
